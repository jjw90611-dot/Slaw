{
  "name": "safety-law-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute safety-law-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
